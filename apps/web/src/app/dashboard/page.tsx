import RoutePlaceholder from '@/components/layout/RoutePlaceholder';

export default function DashboardOverviewPage() {
  return (
    <RoutePlaceholder
      route="/dashboard"
      description="Overview of your channel's performance — reach, viewers and live stream health. No reference image exists yet, so this page stays a placeholder until one is added."
    />
  );
}