import { RoutePlaceholder } from '@/components/route-placeholder';

export default function Page() {
  return <RoutePlaceholder route="/dashboard/stream" description="Stream setup and controls." />;
}
