import { RoutePlaceholder } from '@/components/route-placeholder';

export default function Page() {
  return <RoutePlaceholder route="/dashboard/settings" description="Account and channel settings." />;
}
