import RoutePlaceholder from '@/components/layout/RoutePlaceholder';

export default function DashboardAnalyticsPage() {
  return (
    <RoutePlaceholder
      route="/dashboard/analytics"
      description="Detailed analytics for your broadcasts — viewer timelines, watch time and per-stream numbers. No reference image exists yet, so this page stays a placeholder until one is added."
    />
  );
}