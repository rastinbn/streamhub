import RoutePlaceholder from '@/components/layout/RoutePlaceholder';

export default function HelpPage() {
  return (
    <RoutePlaceholder
      route="/help"
      description="Frequently asked questions, guides and support resources. No reference image exists yet, so this page stays a placeholder until one is added."
    />
  );
}