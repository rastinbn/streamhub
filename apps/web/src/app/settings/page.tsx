import RoutePlaceholder from '@/components/layout/RoutePlaceholder';

export default function SettingsPage() {
  return (
    <RoutePlaceholder
      route="/settings"
      description="Account, notification and stream preferences. No reference image exists yet, so this page stays a placeholder until one is added."
    />
  );
}